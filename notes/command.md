## to see all the logs while building docker image and with no chache

```
docker build --no-cache --progress=plain -t manishboard-image .
```

## docker compose for only building the image
```
docker-compose build
```